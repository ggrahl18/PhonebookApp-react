{
  "name": "backend",
  "version": "0.0.1",
  "description": "phonebook backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../../part2/phonebookFrontend && npm run build --prod && cp -r build ../../round2/phonebookBackend/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ggrahl18/FSOround2.git"
  },
  "author": "garrett",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ggrahl18/FSOround2/issues"
  },
  "homepage": "https://github.com/ggrahl18/FSOround2#readme",
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.9",
    "morgan": "^1.10.0"
  }
}
